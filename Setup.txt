cài venv bản python 3.11

cài postgresql. Tạo database: diem_danh
(user: postgres, pass: 123456)

trong database tạo 2 bảng:
CREATE TABLE IF NOT EXISTS public.attendance_logs (
    id INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    user_code VARCHAR(50),
    checkin_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    image_path TEXT,
    score DOUBLE PRECISION
);

CREATE TABLE IF NOT EXISTS public.user_faces (
    id INTEGER GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    user_code VARCHAR(50) UNIQUE,
    full_name VARCHAR(100),
    embedding DOUBLE PRECISION[],
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);


cài thư viện trong file requirements.txt: 
	Lưu ý: Trong venv, chạy lệnh: pip install requirements.txt
